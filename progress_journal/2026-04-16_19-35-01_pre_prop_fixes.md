# Progress Journal — 2026-04-16_19-35-01 (Pre-Proposition Fixes 1-3)

**Author:** Nidhi Rajani  
**Session:** Fix section title corruption, overlap metadata/dedup strategy, and missing table content

---

## Fix 1 — Section title corruption (`A`/`B`)

### Problem
Some chunks had `section_title` values like `A` and `B` (1-2 chars), especially around index-like pages.

### Code changes
- `ingestion/extract.py`
  - Added `_sanitize_section_title(...)`
  - During section->seed mapping, if `section_title` length <= 2:
    - replace with `chapter_title + section_num` when available
    - else fallback to `chapter_title`
- `ingestion/chunk.py`
  - Added same section-title sanitizer in chunk construction and normalization.

### Verification
- `Chunks with section_title <= 2 chars: 0`

---

## Fix 2 — Overlap dedup strategy + `is_overlap`

### Problem
Overlap chunks were not explicitly flagged for downstream retrieval dedup logic.

### Code changes
- `ingestion/chunk.py`
  - Base chunks now carry `is_overlap = false`
  - Overlap chunks now carry `is_overlap = true`
  - Added `normalize_chunk_fields(...)` to backfill/normalize this on existing artifacts.
- `retrieval/retriever.py`
  - Added `_dedup_overlap_vs_base(results)` helper:
    - if overlap and base are both present -> keep higher score
    - if only overlap present -> keep overlap
    - if only base present -> keep base
  - documented usage point in retrieval flow TODO.

### Verification
- `base_chunks_is_overlap_false: 3944`
- `overlap_chunks_is_overlap_true: 3857`
- `missing_is_overlap_field: 0`

---

## Fix 3 — Missing table content

### Problem
Section-based parser does not capture tables by default. Critical anatomy information in tables was missing from index.

### Diagnostics
- Raw PDF table count (`pdfplumber.extract_tables`): **381**

### Code changes
- `ingestion/chunk.py`
  - Added table extraction branch:
    1. Extract raw tables with `pdfplumber`
    2. Convert each table to prose with Claude using `cfg.prompts.table_to_text`
    3. Filter garbage with `_table_reject_reason(...)`
    4. Map table page->section metadata via section page ranges (`_build_page_section_lookup`)
    5. Emit `element_type = "table"`, `is_overlap = false`
  - Added conversion retry helper `_convert_table_to_text(...)`.

### Run outcome
- Table extraction stats:
  - `total_tables_in_pdf: 381`
  - `filtered_raw: 64`
  - `filtered_converted: 0`
  - `table_chunks_added: 59`

### Table keyword checks
- `axillary nerve` in table chunks: `0`
- `deltoid` in table chunks: `0`
- `origin` in table chunks: `11`
- `insertion` in table chunks: `0`

Notes:
- Table chunk count target (>50) is met.
- Anatomy terms like `origin` are present; `deltoid/axillary nerve` did not appear in extracted table prose for this source extraction path.

---

## Requested post-fix checks

- Chunks with `section_title <= 2`: **0** ✅
- `is_overlap=true` vs `is_overlap=false`: **3857 vs 3944** ✅
- `element_type="table"` count: **59** (>50) ✅
- `axillary nerve` in table chunks: **0**
- `deltoid` in table chunks: **0**
- Total chunks (base + overlap + table): **7801**

### Re-run Test 6 (all chunk types combined)
- `axillary nerve: 4` ✅
- `deltoid: 24` ✅
- `rotator cuff: 14` ✅
- `biceps: 33` ✅

---

## Current artifact state
- `data/processed/chunks_ot.jsonl` updated in place
- Backup created before table append:
  - `data/processed/chunks_ot_before_table_fix_backup.jsonl`

Propositions step still **NOT RUN** (awaiting explicit user approval).
