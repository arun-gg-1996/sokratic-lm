# Progress Journal — 2026-04-12_22-09-33

**Author:** Arun Ghontale  
**Session:** Schema lockdown + scraped diagram integration + E2E test plan

---

## What was done

### Scraped diagram integration clarified
- Scraped images (data/diagrams/) serve two roles: knowledge base enrichment + UI visual aids
- `ingestion/index.py` updated: `index_diagrams()` converts diagram metadata JSONs into propositions, indexed alongside textbook propositions in same `sokratic_kb` collection
- `element_type="diagram"` + `image_filename` in payload so UI can display image when diagram chunk is retrieved
- `ui/app.py` updated: shows diagram image when retrieved_chunks contains diagram chunk
- ARCHITECTURE.md Phase 1 Step 5 updated to document two-source indexing

### Canonical schemas defined (`schemas.py`) — new file
All pipeline contracts locked in one place:
- `ChunkSchema`: 10 fields including `chapter_num`, `subsection_title`, `element_type`, `domain`
- `PropositionSchema`: inherits chunk fields + `parent_chunk_text` (in Qdrant payload, no lookup needed) + `image_filename`
- `DiagramStructureEntry` + `DiagramSchema`: `structure_type`, `action`, `innervation`, `clinical_note` — clinical_note critical for OT patient scenarios
- `RetrievedChunkSchema`: what lands in TutorState["retrieved_chunks"]
- `validate_chunk()`, `validate_proposition()`, `validate_diagram()` helpers

### config.yaml updates
- BM25 paths explicit: `bm25_ot`, `bm25_physics` — path inconsistency fixed
- Physics chunk/proposition paths added
- New `qdrant:` section: `vector_size: 3072`, `distance: Cosine`, `on_disk: false`
- `diagrams:` path added

### ARCHITECTURE.md updates
- New **Data Schemas** section with tables for all schemas + Qdrant spec
- **End-to-end platform testing** section added (8 flows: happy path, hints exhausted, sycophancy, leak attempt, multimodal, cross-session memory, domain swap, out-of-scope)
- Table extraction tests added to Module 1 testing plan
- Phase numbering fixed (duplicate Phase 12 resolved)
- `flag_answer_leak` correctly removed from MCP tools table
- `faiss_top_k` → `qdrant_top_k`, `proactive_revisit_every_n` removed from config block

### Chunking discussion
- Decided: keep semantic chunking + proposition extraction for v1 (late chunking not needed — GPT-4o already resolves pronouns during proposition extraction)
- Table extraction sanity check added to `test_ingestion.py`: per-chapter table count report, hard fail if total < 30

## Status
Plan is tight enough for Codex to start RAG implementation.

## Next up
- Start ingestion implementation: `extract.py` → `chunk.py` → `propositions.py` → `build_structure.py` → `index.py`
- Hand off to Codex with: stub files + schemas.py + ARCHITECTURE.md Phase 1 section + config.yaml
