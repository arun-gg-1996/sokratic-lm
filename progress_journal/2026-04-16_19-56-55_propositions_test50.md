# Progress Journal — 2026-04-16_19-56-55 (propositions test, first 50 base chunks)

**Author:** Nidhi Rajani  
**Session:** Phase 1 Step 4 — `ingestion/propositions.py` reliability pass + 50-base-chunk test

---

## What was implemented

Updated `ingestion/propositions.py` to enforce the requested reliability and scope rules:

1. Base-chunk-only processing
- Filters chunks to `is_overlap == false`
- Skips `element_type == "paragraph_overlap"` entirely

2. Rate-limit and API error handling
- `anthropic.RateLimitError` => wait 60s and retry same chunk
- Any other API exception => log + skip chunk + continue

3. Processing cadence
- One chunk at a time
- `sleep(1)` between chunks

4. Progress output
- Prints per chunk: `Chunk X/50 | propositions so far: Y`

5. Checkpoint saving
- Saves JSONL after every 10 chunks

6. Resume support
- On startup, loads existing `data/processed/propositions_ot.jsonl`
- Builds processed chunk-id set from `parent_chunk_id`
- Skips already-processed chunks to avoid duplication

7. Proposition schema completeness
- Ensures all required `PropositionSchema` fields are written, including `parent_chunk_text`

---

## Run command

```bash
./venv/bin/python -m ingestion.propositions --limit 50 --output data/processed/propositions_ot.jsonl
```

## Run results

- Model: `claude-sonnet-4-6`
- Target chunks: 50 (first 50 base chunks)
- New propositions added: 474
- Avg propositions/chunk: 9.48
- API error chunks: 0
- Zero-proposition chunks: 0

---

## Proposition quality checks (for those 50 target chunks)

1. Atomic check (`" and "` > 2 occurrences)
- Count: 1 / 474 (0.21%)

2. Pronoun-start propositions
- Count: 1 / 474 (0.21%)

3. Length check
- `<10 words`: 143 / 474 (30.17%)
- `>50 words`: 0 / 474 (0.00%)

4. Parent chunk linkage
- Orphan propositions: 0

5. Deltoid spot check
- No chunk containing `deltoid` was present inside the **first 50 base chunks**.
- Deltoid manual spot-check will require selecting a later chunk subset containing Chapter 11 content.

---

## Notes

- First attempt inside restricted sandbox failed with connection errors; rerun with network escalation succeeded.
- Output file updated: `data/processed/propositions_ot.jsonl`.

## Next step

- Run a targeted deltoid manual spot-check chunk (outside first-50 window) for proposition atomicity approval, then await explicit approval before any full-run.
