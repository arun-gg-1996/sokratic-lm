# Progress Journal — 2026-04-17_18-25-49

**Author:** Arun Ghontale  
**Date:** 2026-04-17 18:25 EDT  
**Session:** Token-budget handoff (tutoring + clinical flow fixes)

---

## Purpose

Quick handoff of the **next required fixes** from the latest live run.  
No product/UI code changes were made in this step; this is documentation only.

---

## Quick review (few words)

- Good: topic cards worked, non-happy path progressed, answer eventually reached.
- Good: hint display moved to `2/3` and flow continued without crashing.
- Bad: tutor gave overly strong affirmation on a wrong/partial step.
- Bad: hint progression felt sticky (not reliably reaching `3/3` for repeated wrong guesses).
- Bad: clinical application closes after one student response (should be multi-turn coaching).
- Bad: clinical opt-in area still feels slightly duplicated/disconnected in messaging.

---

## Next fixes required (priority order)

### P0 — Tutoring correctness and hint behavior
1. **No false affirmation on wrong/partial replies**
   - Enforce deterministic guard: if `student_state != correct`, block “exactly/correct/perfect” style openings.
2. **Reliable hint escalation to 3/3**
   - Wrong nerve guesses must classify as `incorrect` (not partial-by-default), so hint level increments reliably.
3. **True final-push behavior at hint 3**
   - Use short, direct, non-revealing prompt template specifically for level 3.

### P0 — Clinical application must be multi-turn
4. **Add clinical turn state**
   - Track `clinical_turn_count`, `clinical_hint_level`, and `clinical_student_state`.
5. **Do not end clinical path after one response**
   - End only when:
     - clinical answer is correct (with confidence threshold), or
     - max clinical turns reached (e.g., 2–3 turns).
6. **Per-turn clinical coaching quality**
   - After each clinical response, return:
     - what was correct,
     - what was incorrect/missing,
     - one concrete next reasoning step.

### P1 — Flow coherence cleanup
7. **Clinical opt-in text coherence**
   - Keep one connected transition (avoid two disjoint prompts/cards saying similar things).
8. **Topic card stage copy cleanup**
   - Keep only card-driven prompt text; avoid duplicate instruction blocks.

### P1 — Debug/export observability
9. **Export should capture closure reason**
   - Include whether session ended due to:
     - tutoring reached-answer,
     - clinical correct,
     - clinical max-turn exit.

---

## Suggested acceptance checks (fast)

1. Non-happy path: two wrong attempts + one correct should reach hint `3/3` before final correct response.
2. No “exactly/correct” wording appears when student response is wrong/partial.
3. Clinical path with partial first response should continue at least one follow-up turn.
4. Final mastery summary should reflect clinical progression, not single-shot closure.

---

## Status at handoff

- Main unresolved functional gap: **clinical section is still single-turn**.
- Highest-priority functional work: **classification strictness + hint escalation + multi-turn clinical loop**.
