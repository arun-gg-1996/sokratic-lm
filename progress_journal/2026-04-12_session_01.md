# Progress Journal — 2026-04-12

**Author:** Arun Ghontale  
**Session:** Initial Architecture Planning

---

## What was done

- Finalized Dean-Teacher architecture: Dean holds locked_answer, Teacher never sees it
- Removed FAISS entirely — Qdrant is the only vector store (two collections: `sokratic_kb` + `sokratic_memory`)
- Designed 3-level LeakGuard: exact match → cosine similarity (embeddings) → GPT-4o entailment (in Dean's normal call, no extra API cost)
- Added hard turn rule: `if turn_count < 3: ALWAYS FAIL` — hardcoded course requirement
- Designed 9 guardrails, documented in GUARDRAILS.md
- Created full project scaffold: all stub files in place (ingestion, retrieval, conversation, memory, tools, multimodal, evaluation, simulation, ui)
- Set config.yaml as single source of truth for all thresholds, k-values, and prompts
- Established ingestion pipeline design: extract → chunk (LlamaIndex semantic) → propositions (GPT-4o) → index (Qdrant + BM25 pkl)
- Cross-encoder scores (query, parent_chunk) pairs — NOT (query, proposition); dedup by parent_chunk_id first
- Weak topics anchored to chapter/section/subsection metadata → generalizable across domains
- 6 student profiles designed (S1–S6): Strong, Moderate, Weak, Overconfident, Disengaged, Anxious
- Assessment phase: mastery summary path (student answered) vs reveal path (hints exhausted)
- Progress journal system set up

## Next up

- Step 2: Implement ingestion pipeline (extract.py → chunk.py → propositions.py → build_structure.py → index.py)
- Curate 40–50 anatomical diagrams into `data/diagrams/`
- Start Qdrant Docker container
- Install dependencies
