# Progress Journal — 2026-04-12_20-49-56

**Author:** Arun Ghontale  
**Session:** Model decisions + vision flow + blind test

---

## What was done

### Model stack finalized
- All LLM calls → `claude-sonnet-4-5` (Dean, Teacher, vision, propositions, summarizer)
- Embeddings → `text-embedding-3-large` (OpenAI, RAG only — ingestion + query + LeakGuard level 2)
- Cross-encoder → local `cross-encoder/ms-marco-MiniLM-L-6-v2` (no API cost)
- Vision → `claude-sonnet-4-5` natively (no separate vision model needed)
- `requirements.txt` updated: `anthropic` added, `langchain-openai` removed

### Vision flow clarified
- `retriever.retrieve(structure_name)` checks against `sokratic_kb` propositions — same pipeline as text queries
- Diagram metadata JSONs also indexed in `sokratic_kb`
- `locked_answer` not set on image upload — Teacher asks focusing question first
- Gap identified: Teacher prompt needs a conditional block for `is_multimodal=True` entry point (to be added)

### Blind test evaluation added (project spec requirement)
- `evaluation/blind_test.py` stub created
- `data/eval/blind_test/images/` + `ground_truth.json` structure defined
- ~10 held-out unlabeled diagrams needed when curating diagram set
- Target: mean recall ≥ 0.75 on structure identification
- Added to development order (Step 10 gate)

### Progress journal tracker
- Moved from Stop hook to manual `_tracker.json` approach (hook wasn't firing per-turn)
- Counter resets every 6 turns, I update it on every response

## Next up
- Discuss and finalize vision flow in detail
- Add `is_multimodal` conditional block to Teacher prompt in `config.yaml`
- Start ingestion implementation
