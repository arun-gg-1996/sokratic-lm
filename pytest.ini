[pytest]
asyncio_mode = auto
markers =
    integration: real LLM calls, requires ANTHROPIC_API_KEY
    scenarios: multi-turn conversation scenarios, requires ANTHROPIC_API_KEY
    asyncio: async test (handled by pytest-asyncio in auto mode)
