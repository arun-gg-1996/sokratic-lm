# Core LLM / orchestration
langchain==0.3.25
langgraph==0.2.60
anthropic==0.52.2
openai>=1.108.1

# Retrieval
# faiss-cpu removed — Qdrant replaces FAISS
rank-bm25==0.2.2
rapidfuzz==3.10.1
sentence-transformers==3.4.1
# scispacy: biomedical NER + UMLS entity linker. First use downloads
# ~1GB UMLS KB to ~/.scispacy/. Required only for anatomy/OT domain;
# other domains (physics, etc.) get a NoopAdapter and never touch this.
# Model: install en_core_sci_sm==0.5.4 from the scispacy S3 wheel:
#   pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_sm-0.5.4.tar.gz
scispacy==0.5.5
spacy==3.7.5

# Chunking / ingestion
llama-index>=0.14.21
llama-index-core>=0.14.21
pymupdf==1.25.5
pdfplumber==0.11.6

# Memory
mem0ai==0.1.103
qdrant-client==1.17.1

# Web server / API
# 2026-05-06 deploy-readiness: fastapi + uvicorn were transitively pulled
# in via dev installs but were never declared. The VM bootstrap won't
# install them unless they're listed here. Pinning to currently-validated
# versions; uvicorn[standard] picks up uvloop + httptools for prod perf.
fastapi==0.136.0
uvicorn[standard]==0.44.0
pydantic==2.13.1
websockets==16.0
httpx==0.28.1
# python-multipart: required by FastAPI for VLM upload form parsing
# (backend/api/vlm.py UploadFile). Loose pin in backend/requirements.txt;
# this hard-pin is the production-of-record version.
python-multipart==0.0.26

# NLP utilities — required by retrieval (PorterStemmer in retriever.py)
nltk==3.9.4

# HuggingFace artifact distribution — used by scripts/bootstrap_corpus.py
# (idempotent VM-side pull) and scripts/publish_corpus.py (push).
huggingface_hub==0.36.2

# UI

# Evaluation
ragas==0.2.14

# ML (cross-encoder only — no local LLM)
torch==2.7.0
transformers==4.51.3

# Async + utilities
tqdm==4.67.1
aiohttp==3.11.18
pyyaml==6.0.2

# Testing
pytest==8.3.5
pytest-asyncio>=1.3.0
python-dotenv==1.1.0
